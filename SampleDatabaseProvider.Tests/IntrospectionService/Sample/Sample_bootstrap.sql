CREATE TABLE IntrospecTbl%Machine% (ID int NOT NULL GENERATED BY DEFAULT AS IDENTITY, DBTEXT varchar(255) NOT NULL, DBINTEGER int, DBLONGINTEGER bigint, DBDECIMAL decimal(28, 8), DBBOOLEAN smallint, DBDATETIME timestamp, DBBINARYDATA blob, PRIMARY KEY (ID), CONSTRAINT CHECK_DBBOOLEAN%Machine% CHECK (DBBOOLEAN in(0,1)))

CREATE VIEW IntrospecView%Machine% AS SELECT * FROM IntrospecTbl%Machine%

CREATE TABLE "SELECT%Machine%" (TEXTID varchar(10) NOT NULL, VAL int, PRIMARY KEY (TEXTID))

CREATE VIEW "FROM%Machine%" AS SELECT * FROM "SELECT%Machine%"

CREATE TABLE ForeignKeysTable%Machine% (ID int, INTROSPECTIONID int, SELECTID varchar(10) NOT NULL, constraint FK_FKT_IspecTbl%Machine% FOREIGN KEY (INTROSPECTIONID) REFERENCES IntrospecTbl%Machine%(ID), constraint FK_FKT_Select%Machine% FOREIGN KEY (SELECTID) REFERENCES "SELECT%Machine%"(TEXTID))
